/**
 * Enum that represents the priority of the current log event.
 * One being the highest priority until five which is the lowest priority.
 */
export enum LogVerbosity {
    One = 1,
    Two = 2,
    Three = 3,
    Four = 4,
    Five = 5
}
